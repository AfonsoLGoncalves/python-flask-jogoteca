# image:
#   file: .gitpod.Dockerfile

ports:
  - port: 5000
    onOpen: open-preview

tasks:
  init: |
    python3 -m venv venv
    . venv/bin/activate
  command: |
    pip install Flask
    python jogoteca.py
vscode:
  extensions:
    - ms-python.python
